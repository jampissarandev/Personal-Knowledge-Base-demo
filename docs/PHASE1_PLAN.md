# Phase 1 — Backend Foundation Plan & Status

> Working plan for Phase 1 of `docs/PLAN.md` (lines 263–272).
> Generated by orchestrator on 2026-06-19. Updated as sub-agents report.

## 1. Scope Recap (from `docs/PLAN.md` §8)

1. Create .NET 10 Web API project + EF Core + SQLite
2. Configure Identity + JWT in `Program.cs`
3. Create domain models + `AppDbContext` + first migration
4. Implement `AuthController` (register + login)
5. Add Serilog (file + console)
6. Verify with Swagger

## 2. Current State (survey 2026-06-19)

| Item | Status | Evidence |
|---|---|---|
| .NET 10 Web API project | ✅ done | `server/PersonalKnowledgeBase.Api/PersonalKnowledgeBase.Api.csproj` (TargetFramework=net10.0) |
| EF Core 10 + SQLite | ✅ done | `Microsoft.EntityFrameworkCore.Sqlite 10.*` in csproj, `AppDbContext` registered in `Program.cs:57-58` |
| Identity + JWT | ✅ done | `Program.cs:60-71` (Identity), `Program.cs:94-123` (JWT Bearer) |
| Domain models | ✅ done | `Models/ApplicationUser.cs`, `Note.cs`, `Tag.cs`, `Folder.cs`, `NoteTag.cs` |
| `AppDbContext` | ✅ done | `Data/AppDbContext.cs` (relationships, indexes) |
| First migration | ✅ done | `Migrations/20260619064454_InitialCreate.cs` |
| `AuthController` (register/login) | ✅ done | `Controllers/AuthController.cs` (also adds `/me`) |
| DTOs | ✅ done | `DTOs/Auth/{RegisterRequest,LoginRequest,AuthResponse,UserResponse}.cs` + `ErrorResponse.cs` |
| `AuthService` + `TokenService` | ✅ done | `Services/AuthService.cs`, `Services/TokenService.cs` |
| Serilog (file + console) | ✅ done | `Program.cs:15-25`, `Logs/app-20260619.log` exists |
| Exception middleware | ✅ done | `Middleware/ExceptionHandlingMiddleware.cs` |
| **Migration applied (DB exists)** | ❌ **TODO** | `app.db` missing from `server/PersonalKnowledgeBase.Api/` |
| **JWT key real (not placeholder)** | ❌ **TODO** | `appsettings.json` has `REPLACE_WITH_SECURE_KEY_...` |
| **Build green** | ❌ **TODO** | not yet verified end-to-end |
| **Swagger reachable** | ❌ **TODO** | not yet verified |
| **Endpoint smoke test** | ❌ **TODO** | register / login / me not yet exercised |
| **Security review** | ❌ **TODO** | security-reviewer not yet run |
| **Code review** | ❌ **TODO** | code-reviewer not yet run |

## 3. Orchestration Plan

Per `agents/AGENTS.md`, follow DEFINE → PLAN → BUILD → VERIFY → REVIEW.

### Step A — backend-engineer (sequential, must run first)
**Objective:** eliminate the 4 TODO items above so downstream verifiers have something real to test.

1. Apply the existing `InitialCreate` migration to materialise `app.db`.
2. Replace the placeholder `JwtSettings:Key` in `appsettings.Development.json` with a real 64-char dev key (do NOT touch `appsettings.json` — that's the prod template).
3. Add a one-line `data:` comment to `appsettings.json` clarifying the prod key MUST be overridden via env var.
4. Build the project to confirm it still compiles.
5. Do NOT change runtime behavior; do NOT add features.

**Acceptance:**
- `server/PersonalKnowledgeBase.Api/app.db` exists.
- `appsettings.Development.json` contains a real 64+ char key.
- `dotnet build` exits 0.
- Report: list of files changed + exit codes.

### Step B — parallel fan-out (qa-verifier, security-reviewer, code-reviewer)
Per `docs/agents.md` orchestration pattern, run three reviewers concurrently. Each gets the codebase snapshot AFTER Step A lands.

1. **qa-verifier** — build, run, hit endpoints with curl, confirm log file.
2. **security-reviewer** — STRIDE threat model of auth surface, JWT hygiene, OWASP top 10.
3. **code-reviewer** — five-axis review (correctness/readability/architecture/security-handoff/performance).

### Step C — merge
Orchestrator synthesises the three reports into a single Phase 1 verdict for the user.

## 4. Verification (end-of-phase)

- [ ] Build exits 0
- [ ] `/health` returns 200
- [ ] `/swagger/index.html` returns 200
- [ ] `POST /api/auth/register` returns 200 with JWT
- [ ] `POST /api/auth/login` returns 200 with JWT
- [ ] `GET /api/auth/me` with Bearer returns 200 with user
- [ ] `Logs/app-20260619.log` contains `user_registered` and `user_login` events
- [ ] Security review: no Critical findings open
- [ ] Code review: APPROVE or APPROVE WITH NITS

## 5. Out of Scope (deferred to later phases)

- Notes / Tags / Folders controllers (Phase 2)
- SQLite FTS5 (Phase 3)
- Frontend (Phases 4–7)
- Production secrets management (deferred — `appsettings.json` keeps the placeholder)
